dataset:
  _target_: datasets.netcdf_vertical.NetCDFVerticalSliceDataset
  files: []  # list of netCDF file paths
  variable: image
  mask_variable: mask
  coverage_threshold: 0.0

datamodule:
  _target_: training.datamodule.NetCDFDataModule
  dataset_cfg: ${dataset}
  batch_size: 1
  num_workers: 0
